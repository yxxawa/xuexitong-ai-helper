name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    name: Typecheck and build
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.30.3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Build
        run: pnpm build

      - name: Upload userscript artifact
        uses: actions/upload-artifact@v4
        with:
          name: xuexitong-ai-helper-userscript
          path: dist/xuexitong-ai-helper.common.user.js
