# easy-us

随项目保留的补丁版运行库及类型声明，来源与许可见根目录 NOTICE.md。

本地补丁包括学习通侧边导航、直接页面切换及过期渲染保护、配置监听清理、精确域名匹配和窗口位置校正。

不要直接用 npm 原版覆盖本目录；浏览器回归测试会覆盖这些行为。

- GM 标签页信号按 tab UID 隔离，只在本标签页离开时清理自身信号。启动新标签页（包括 DeepSeek 接收端重载）不再删除其他标签页的导航/答题结果监听信号。
